{
  "name": "quick-struct",
  "version": "0.3.8",
  "main": "./build/index.js",
  "types": "./types/index.d.ts",
  "author": "aokihu <aokihu@gmail.com>",
  "license": "MIT",
  "keywords": [
    "bin",
    "binary",
    "convert",
    "arraybuffer",
    "buffer",
    "struct",
    "c",
    "rust",
    "encode",
    "decode",
    "big-endian",
    "little-endian",
    "endianness"
  ],
  "description": "this is library convert binary to javascript object, it's easy to write struct description, it is like C-style struct",
  "homepage": "https://github.com/aokihu/quick-struct",
  "maintainers": [
    "aokihu@gmail.com"
  ],
  "publisher": "aokihu@gmail.com",
  "bugs": {
    "email": "aokihu@gmail.com",
    "url": "https://github.com/aokihu/quick-struct/issues"
  },
  "scripts": {
    "prepublish": "rm -f build && yarn build",
    "prepack": "rm -rf build && yarn build",
    "build": "npx esbuild src/index.ts --minify --bundle --platform=node --target=node16 --outfile=build/index.js",
    "pretest": "npx tsc && npx esbuild src/index.ts --sourcemap --bundle --platform=node --target=node16 --outfile=build/index.js ",
    "test": "npx mocha",
    "bench": "yarn build && node benchmark/index.mjs"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "esbuild": "^0.14.32",
    "mocha": "^9.2.2",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.3"
  }
}