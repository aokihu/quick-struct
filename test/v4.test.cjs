const {qs} = require('../build/index.js')

describe("Encode testcases", () => {

    it("", () => {
        const struct = qs`
            struct {
                u8 a;
            }
        `
    })


})